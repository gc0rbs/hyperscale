# indexer

Ponder project for leaderboards and history (docs/06 §5). Phase 3 replaces the type sketch in
`src/schema.ts` with a real `ponder.schema.ts` and event handlers once the contract ABI exists.
